import { Meta } from '@storybook/blocks';
import LeafHome from "../../assets/images/logo.svg";

<Meta title='Introduction' />

<img src={LeafHome} alt="LeafHome logo" className="sb-explore-image" width="300"/>

# Digital Polygon Starter Theme

This starter theme has twig, storybook and laravel mix integration and should can be used to build components and pages that adhere to the Atomic design system.
